[tools]
python = "3.11"
uv = "0.11"

[settings]
experimental = true
python.uv_venv_auto = true

[hooks]
postinstall = "uv sync --extra cli"

[tasks]
check = ["uv sync --extra cli", "ruff check", "mypy", "pytest"]
